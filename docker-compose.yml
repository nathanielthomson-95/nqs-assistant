services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: nqs
      POSTGRES_PASSWORD: localdev
      POSTGRES_DB: nqs
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: